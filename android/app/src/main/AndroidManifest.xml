<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Default small icon/color for push notifications (FCM falls back
             to auto-flattening the launcher icon if these aren't set, which
             typically renders as a plain white blob in the status bar). -->
        <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/ic_stat_notify" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_color" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for recording voice messages in chat. MODIFY_AUDIO_SETTINGS
         accompanies RECORD_AUDIO for WebView getUserMedia on some devices. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- MANDATORY on Android 13 (API 33) and above. Without this declared,
         the OS silently refuses to display any notification, no matter how
         correct the channel, token, and payload are. This was almost
         certainly why notifications never appeared on the phone. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets audio keep playing while the app is backgrounded. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required for the media notification (artwork, scrubber, transport
         controls) to appear in the shade and survive backgrounding. Android 14+
         additionally requires the mediaPlayback service type to be declared. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
</manifest>
